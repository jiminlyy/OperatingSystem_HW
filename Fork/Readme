exec()는 자식의 프로세스에서 현재 프로세스에서 메모리를 새 프로그램으로 덮어쓴다
PID는 유지되고, 코드/데이터/힙/스택이 완전히 교체된다

exec()를 사용하면 그 뒤에 코드는 다시 실행되지 않는다.
PID와 열린 파일은 유지한다. 


exec() 와 fork()를 분리함으로서
파일 디스크립터를 조작해서 입출력을 자유롭게 변경할 수 있게 되었다. 
[splitingForkExec.cpp]
-> 이 코드 의미:
* 자식이 fd 1을 파일로 바꿔 놓고
* ls를 실행
* 그러면 ls 출력이 output.txt로 감